# 软件源说明

固件使用 ImmortalWrt 24.10-SNAPSHOT 在线软件源，路由器实测可用。

## 在线安装

```sh
opkg update
opkg install <package>
```

## 内核模块 (kmod)

内核模块必须来自同一次构建。从 Release 下载 `kmod-packages.tar.gz`，
解压后本地安装。24.10-SNAPSHOT 的 kmod (kernel 6.6) 与当前固件 (kernel 6.18) 不兼容。

# 软件源说明

本固件使用 **APK** 作为主包管理器，同时保留 **OPKG** 用于本地 `.ipk` 安装。

## 在线软件源 (APK)

固件的 `/etc/apk/repositories` 由构建时自动生成，指向官方 ImmortalWrt Snapshot。
`apk update` 和 `apk add` 可直接使用。

## 本地 IPK 安装 (OPKG)

保留 `opkg` 命令行工具，用于安装本地 `.ipk` 文件：
```sh
opkg install /tmp/package.ipk
```

## 内核模块

**禁止**从在线源安装内核模块 (kmod)。APK snapshots 的 kernel 版本可能与
当前固件不一致。内核模块必须与固件的 kernel vermagic 完全一致。
外部 kmod 可能导致内核崩溃或启动失败。

## 兼容版本

| 来源 | APK 在线 | OPKG 本地 | kmod |
|------|:--:|:--:|:--:|
| ImmortalWrt Snapshot (APK) | ✅ | — | ⚠️ vermagic |
| 同构建 artifacts .ipk | — | ✅ | ✅ |
| 24.10-SNAPSHOT (OPKG) | — | ❌ ABI | ❌ |
| 其他发行版 | ❌ | ❌ | ❌ |

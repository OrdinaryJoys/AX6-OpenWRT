# 软件源说明

固件的 `/etc/opkg/distfeeds.conf` 由当前锁定源码在构建时生成，不在
`AX6-IPQ/files` 中覆盖。这样目标、架构和源码版本会保持一致。

不要把 ImmortalWrt 24.10 或其他发行版的 `distfeeds.conf` 整体复制到本固件。
本构建使用自定义 qualcommax 6.18 内核和 NSS 组件，第三方仓库中的 kmod 必须与
运行中内核的版本及 vermagic 完全一致，否则安装会失败，强制安装还可能导致启动
或网络故障。

自建附加软件源时只添加明确兼容的用户态包源，并在安装前检查依赖。内核模块应从
同一次固件构建产生的 artifacts 中取得。

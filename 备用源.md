# 软件源说明

本固件使用 OPKG 包管理器，不提供在线软件源。

## 本地安装

使用 `opkg install /tmp/package.ipk` 本地安装。

## 内核模块

内核模块必须与固件来自同一次构建。kernel vermagic 必须完全一致。
外部 kmod 可能导致内核崩溃或启动失败。

## 不兼容的源

- ImmortalWrt Snapshot 已迁移到 APK 格式
- 24.10 及衍生版本 ABI 与当前固件不兼容
- 其他发行版的包不应混用

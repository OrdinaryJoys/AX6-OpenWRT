# 软件源说明

本固件的 `/etc/opkg/distfeeds.conf` 已禁用在线软件源。

## 用户态包安装

如需安装额外用户态包，从同一次构建的 Actions artifacts 获取对应架构的 `.ipk` 文件，
使用 `opkg install <package>.ipk` 本地安装。

## 内核模块

**禁止**从外部源安装内核模块 (kmod)。内核模块必须与固件的 kernel vermagic 
(`6.18.35 SMP mod_unload aarch64`) 完全一致。外部 kmod 可能导致内核崩溃或启动失败。

## 不兼容的源

- ImmortalWrt 24.10 及其衍生版本的包与当前固件 ABI 不兼容
- ImmortalWrt Snapshot 已迁移到 APK 格式，与 OPKG 不兼容
- 其他 OpenWrt 衍生发行版的包不应混用

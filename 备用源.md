# 软件源说明

本固件使用 OPKG 包管理器，不提供在线软件源。

## 本地安装

使用 `opkg install /tmp/package.ipk` 本地安装。

## 内核模块

内核模块必须来自同一次构建。从 Release 下载 `kmod-packages.tar.gz`，
解压后本地安装。kernel vermagic 必须完全一致。

## 不兼容的源

- ImmortalWrt Snapshot 已迁移到 APK
- 24.10 及衍生版本 ABI 与当前固件不兼容
- 其他发行版包不应混用

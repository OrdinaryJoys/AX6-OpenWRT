# 软件源说明

固件使用 ImmortalWrt 24.10-SNAPSHOT 软件源。

## 在线安装

`opkg update` 刷新软件列表后，可安装用户态包：
```sh
opkg update
opkg install <package>
```

## 内核模块

**内核模块 (kmod) 必须来自同一次构建**。24.10-SNAPSHOT 的 kmod 编译自
kernel 6.6，与当前固件 kernel 6.18 不兼容。强行安装会导致内核崩溃。

## 兼容性

| 包类型 | 在线 24.10-SNAPSHOT | 同构建 |
|------|:--:|:--:|
| 用户态 (luci/tools等) | ✅ | ✅ |
| kmod (内核模块) | ❌ | ✅ |

# 软件源说明

固件使用 ImmortalWrt 24.10-SNAPSHOT 软件源，路由器实测可用。

## 在线安装

```sh
opkg update
opkg install <package>
```

## 内核模块

内核模块 (kmod) 必须来自同一次构建。从 Release 下载 `kmod-packages.tar.gz`，
解压后 `opkg install` 本地安装。

kernel 6.18 与 24.10-SNAPSHOT (kernel 6.6) 的 kmod 不兼容。
